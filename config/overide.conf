# Unlock NFC pins D7 and D8 for use as GPIO
CONFIG_NFCT_PINS_AS_GPIOS=y

# Stronger Bluetooth signal for better split connection
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable deep sleep to save battery
CONFIG_ZMK_SLEEP=y